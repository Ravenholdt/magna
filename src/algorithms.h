
#pragma once

float calculateDeltaV(float mass, float radius, float atmosphere);
float calculateGravity(float mass, float radius);
float calculateOrbitalPeriod(float mass, float distance);
float calculateOrbitalVelocity(float orbitalPeriod, float distance);